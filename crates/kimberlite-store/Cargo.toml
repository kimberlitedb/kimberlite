[package]
name = "kimberlite-store"
description = "Page-based B+tree projection store with MVCC for Kimberlite"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[features]
# Adds `BTreeStore::reset` for persistent-mode fuzzing. MUST NOT be
# enabled in production builds — reset wipes all persisted data.
fuzz-reset = []

[dependencies]
kimberlite-types.workspace = true
kimberlite-crypto.workspace = true
bytes.workspace = true
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true
test-case.workspace = true
tempfile.workspace = true
