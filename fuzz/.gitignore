target
corpus/*
# Grammar seed corpus is checked-in so EPYC has a deterministic
# starting point on first `just fuzz-epyc-deploy`. Regenerated by
# `just fuzz-seed-sql-grammar`; libFuzzer mutations evolve beyond
# this on the box itself.
!corpus/fuzz_sql_grammar/
!corpus/fuzz_sql_parser_adversarial/
artifacts
coverage
